#include <stdio.h>

/**
 * main - entry point
 * @argc: int
 * @argv: array of strings
 * Return: if error 1; else 0
 */

int main(int argc, char *argv[])
{
	int r, i, num, items;

	for (i = 0; i < argc; i++)
	{
		if 
